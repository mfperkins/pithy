.row
  .col.s12
    %h3
      %strong= @person.first_name + " " + @person.last_name
      \ // New Quote
    = form_for @quote do |f|
      .col.s12
      %h5 What did they say?
      = f.text_field(:text, placeholder: "The quote")
      = f.button 'save quote', class: "btn waves-effect waves-light right"
